<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="fastScrollerTheme">@style/MyScrollerStyle</item>
    </style>

    <style name="MyScrollerStyle">
        <item name="textAppearance">@android:style/TextAppearance.Small</item>
        <item name="bubbleColor">@android:color/white</item>
        <item name="handleColor">@android:color/darker_gray</item>
    </style>

</resources>
